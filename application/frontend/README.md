# Reporting portal

The React/Vite portal demonstrates the analyst upload experience and cycle-status view.

```bash
npm ci
npm run dev
```

Set `VITE_API_URL` to the deployed API URL for upload integration. The current UI uses the local
demo role header; enterprise OIDC sign-in is an explicit production integration step.
